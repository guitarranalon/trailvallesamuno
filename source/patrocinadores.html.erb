---
title: "Patrocinadores"
---

<div id="content-block">
	<h2>Patrocinadores</h2>
	<ul class="patrocinadores">
		<% data.patrocinadores.empresas.each do |empresa| %>
			<li>
				<a href="<%= empresa.link %>" title="<%= empresa.linktitle %>">
					<img src="images/patrocinadores/<%= empresa.image %>" alt="<%= empresa.name %>" />
				</a>
			</li>
		<% end %>
	</ul>

	<% data.colaboradores.oficiales.each do |epigrafeoficial| %>
		<h3><%= epigrafeoficial.title %></h3>
		<ul class="patrocinadores">
			<% epigrafeoficial.empresas.each do |colaboradoroficial| %>
				<li>
					<% if colaboradoroficial.link.nil? || colaboradoroficial.link.empty? %>
						<span>
							<img src="images/colaboradores/<%= colaboradoroficial.image %>" alt="<%= colaboradoroficial.name %>" />
						</span>
					<% else %>
						<a href="<%= colaboradoroficial.link %>" title="<%= colaboradoroficial.linktitle %>">
							<img src="images/colaboradores/<%= colaboradoroficial.image %>" alt="<%= colaboradoroficial.name %>" />
						</a>
					<% end %>
				</li>
			<% end %>
		</ul>
	<% end %>

	<h3>Colaboradores</h3>
	<ul class="patrocinadores">
		<% data.colaboradores.empresas.each do |colaborador| %>
			<li>
				<% if colaborador.link.nil? || colaborador.link.empty? %>
					<span>
						<img src="images/colaboradores/<%= colaborador.image %>" alt="<%= colaborador.name %>" />
					</span>
				<% else %>
					<a href="<%= colaborador.link %>" title="<%= colaborador.linktitle %>">
						<img src="images/colaboradores/<%= colaborador.image %>" alt="<%= colaborador.name %>" />
					</a>
				<% end %>
			</li>
		<% end %>
	</ul>

	<p>
		Si desea colaborar con un patrocinio en el Trail Valle de Samuño puede ponerse en <a href="contacto.html" title="Ir a la sección de contacto">contacto</a> con nosotros.
	</p>
</div>